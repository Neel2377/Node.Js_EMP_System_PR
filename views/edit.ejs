<!DOCTYPE html>
<html lang="en">

<head>
  <title>Edit Employee</title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Glassmorphism CSS -->
  <style>
    body {
      background-color: #f9f9f9;
      font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;
      margin-top: 20px;
    }

    .edit {
      border: 3px solid gray !important;
      padding: 25px;
      border-radius: 15px;
      background-color: #DDDAD0 !important;
    }

    h2 {
      font-size: 1.8rem;
      font-weight: bold !important;
      text-decoration: underline !important;
      margin-bottom: 25px;
      color: #333;
      text-align: center;
    }

    .form-label {
      font-weight: 500;
      color: #000000;
    }

    .form-control {
      border-radius: 6px;
      font-size: 15px;
    }

    .btn-update {
      background-color: #ffc107 !important;
      color: white !important;
      border: 1px solid black !important;
    }

    .btn-cancel {
      background-color: red !important;
      color: white !important;
      border: 1px solid black !important;
    }
  </style>
</head>

<body>

  <div class="container">
    <div class="row">
      <div class="col-md-6 mx-auto">
        <h2>EDIT EMPLOYEE</h2>
        <form action="/employee/edit/<%= employee.id %>" method="POST" class="edit">
          <div class="mb-3">
            <label for="empname" class="form-label fw-bold">Name:</label>
            <input type="text" name="ename" id="empname" class="form-control border border-1 border-secondary"
              value="<%= employee.ename %>" required>
          </div>

          <div class="mb-3">
            <label for="department" class="form-label fw-bold">Department:</label>
            <input type="text" name="department" id="department" class="form-control border border-1 border-secondary"
              value="<%= employee.department %>" required>
          </div>

          <div class="mb-3">
            <label for="salary" class="form-label fw-bold">Salary:</label>
            <input type="number" name="salary" id="salary" class="form-control border border-1 border-secondary"
              value="<%= employee.salary %>" required>
          </div>

          <div class="d-flex justify-content-between">
            <button type="submit" class="btn btn-update">Update</button>
            <a href="/table" class="btn btn-cancel">Cancel</a>
          </div>
        </form>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>